package Strings;
import java.util.Scanner;

public class max_min_occuring_character {
    public static void main(String args[]) {
        int max[]=new int[26];
        Scanner sc=new Scanner (System.in);
        String str=sc.nextLine();
        int count=0;
        for (int i = 0; i < str.length()-1; i++) {
           // if(str.charAt(i)=){

            }

            
        }
    }